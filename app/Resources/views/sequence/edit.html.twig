{% extends '@App/backend/base.html.twig' %}

{% block body %}
    {{ form_start(edit_form,{"attr":{"class":"form-horizontal"}}) }}

    {# Les erraurs générales du formulaire.#}
    {{ form_errors(edit_form) }}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">{{'Edit sequence' | trans  }} {{ sequence.title }}</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
<div class="row">
    <div class="col-lg-8">
    {# Génération du label + erreur + widget pour un champ#}
    {{ form_label(edit_form.title) }}

    </div>
</div>
    {#{{ form_label(edit_form.title) }}#}
        {{ form_widget(edit_form) }}
        <input type="submit" value="{{ 'Edit' | trans }}" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('backend_sequence_index') }}">{{ 'Back to the list' | trans }}</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="{{ 'Delete' | trans }}">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
